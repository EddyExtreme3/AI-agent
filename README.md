# agent

git clone "https://github.com/EddyExtreme3/AI-agent"

python -m venv .venv

pip install -r requirements.txt

# api

- pip install streamlit
- pip install google-auth
- pip install google-auth_oauthlib
- pip install google-api-python-client

# run

streamlit run c:/Users/Abhishek/Desktop/AI-agent/app.py
